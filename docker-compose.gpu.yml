services:
  backend:
    environment:
      - NVIDIA_VISIBLE_DEVICES=${LUMIXAI_VISIBLE_GPUS:-all}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
