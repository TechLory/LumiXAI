[project]
name = "poetry-xai-framework"
version = "0.1.0"
description = ""
authors = [
    {name = "Lorenzo",email = "lorenzo.gatta@studenti.unimi.it"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy (<2.0)",
    "scipy (<1.14)",
    "pandas (>=2.3.3,<3.0.0)",
    "matplotlib (>=3.10.8,<4.0.0)",
    "tqdm (>=4.67.1,<5.0.0)",
    "ipykernel (>=7.1.0,<8.0.0)",
    "jupyter (>=1.1.1,<2.0.0)",
    "streamlit (>=1.52.2,<2.0.0)",
    "transformers (>=4.57.3,<5.0.0)",
    "diffusers (>=0.36.0,<0.37.0)",
    "accelerate (>=1.12.0,<2.0.0)",
    "captum (>=0.8.0,<0.9.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
